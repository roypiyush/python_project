import poc.flask.flask_example as app
# Command to use to start application "uwsgi --enable-threads --ini flask_example.ini"

if __name__ == "__main__":
    # This block is added just to avoid PEP8 error
    app.main()
